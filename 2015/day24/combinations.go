package main

func product(iterable []interface{}, repeat int) chan []interface{} {
	return nil
}

func combinations(iterable []interface{}, length int) chan []interface{} {
	stream := make(chan []interface{})
	//go _combinations(stream, iterable, length)
	return stream
}
